

1. Explain how the `Button` component in this project dynamically handles class names using JavaScript objects and Tailwind CSS.
2. How does Storybook use the `argTypes` object in `stories/Button.stories.jsx` to create an interactive UI control dashboard?
3. Can you explain how this Next.js project is configured to be exported statically to Vercel, and why that might be beneficial for a documentation site?
4. How does the `tailwind.config.js` or CSS configuration handle Dark Mode transitions, and how does the Storybook `preview.js` file emulate this?
5. Walk me through how `babel-loader` and `@babel/preset-react` integrate into Storybook's Webpack configuration via the `.storybook/main.js` file.
6. How can I add an entirely new component to this design system, like a 'Modal' or 'Toast' component? What are the exact files I would need to create?
7. Explain the 'Barrel Export' pattern used in `components/index.js` and why it makes importing components much cleaner for the frontend
